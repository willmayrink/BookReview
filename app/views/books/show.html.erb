<h2><%= @book.title %></h2>
<h3><%= @book.author %></h3>
<p><%= @book.description %></p>

<%= link_to "Voltar", root_path%>
<%= link_to "Editar", edit_book_path(@book)%>
<%= link_to "Apagar", book_path(@book), method: :delete, data: { confirm: "Deseja confirmar a exclusão do livro?"} %>